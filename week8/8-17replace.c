#include <stdio.h>
#include <string.h>

int main(void)
{
    char str[128] = {"Wakayama University is a good university in Wakayama."};
    char query[128];
    char replace[128];
    scanf("%s", query);
    scanf("%s", replace);

    return 0;
}

//too difficult